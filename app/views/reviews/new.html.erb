<div class="container-form">
<h3>Add Your Review for <%= @bar.name %></h3>
<%= simple_form_for [@bar, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Add Review", class: "btn3" %>
  </div>
<% end %>
